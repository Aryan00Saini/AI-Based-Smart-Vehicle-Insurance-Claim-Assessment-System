# Copy this file to .env and set the value for your environment.
# Not needed for local dev (Vite's proxy in vite.config.ts handles it).
# Required when deploying the frontend and backend to different domains
# (e.g. frontend on Vercel, backend on Render).

VITE_API_BASE_URL=https://your-backend-url.onrender.com
